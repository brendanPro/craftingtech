// Common TypeScript interfaces and types
export interface SectionProps {
  id: string;
  index: number;
}

export interface MetaData {
  title: string;
  description: string;
  keywords: string[];
} 